string.indexOf("text"): tìm vị trị của ký tự
lastIndexOf: method tìm chuỗi cuối cùng xuất hiện trong dãy

toPrecision(): làm tròn trong js

Array js:thêm phần tử vào mang method:pop(),push()
.value :lấy giá trị ra
